// Re-export the canonical action catalog from @linkbook/types so the API
// has a single import path. Avoids accidental fork.
export { actionCatalog, type ActionCatalogEntry } from '@linkbook/types';
